NEXT_PUBLIC_SITE_URL=http://localhost:3000
NEXT_PUBLIC_API_URL=http://localhost:4001
NEXT_PUBLIC_STORAGE_HOST=kencana.basic.box.cloudeka.id
# Must be the same value as REVALIDATE_SECRET in the API, or its
# revalidation calls are rejected and edits take up to 5 minutes to appear.
REVALIDATE_SECRET=dev-secret
 
